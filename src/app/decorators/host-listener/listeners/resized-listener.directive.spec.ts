import { ResizedListenerDirective } from './resized-listener.directive';

describe('ResizedListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizedListenerDirective();
    expect(directive).toBeTruthy();
  });
});
