import { MousemoveListenerDirective } from './mousemove-listener.directive';

describe('MousemoveListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new MousemoveListenerDirective();
    expect(directive).toBeTruthy();
  });
});
