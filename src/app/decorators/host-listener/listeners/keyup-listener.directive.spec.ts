import { KeyupListenerDirective } from './keyup-listener.directive';

describe('KeyupListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyupListenerDirective();
    expect(directive).toBeTruthy();
  });
});
