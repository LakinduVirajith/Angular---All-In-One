import { KeydownListenerDirective } from './keydown-listener.directive';

describe('KeydownListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new KeydownListenerDirective();
    expect(directive).toBeTruthy();
  });
});
