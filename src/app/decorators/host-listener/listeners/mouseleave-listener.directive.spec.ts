import { MouseleaveListenerDirective } from './mouseleave-listener.directive';

describe('MouseleaveListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseleaveListenerDirective();
    expect(directive).toBeTruthy();
  });
});
