import { DoubleClickListenerDirective } from './double-click-listener.directive';

describe('DoubleClickListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new DoubleClickListenerDirective();
    expect(directive).toBeTruthy();
  });
});
