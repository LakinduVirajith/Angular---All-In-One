import { MouseenterListenerDirective } from './mouseenter-listener.directive';

describe('MouseenterListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseenterListenerDirective();
    expect(directive).toBeTruthy();
  });
});
