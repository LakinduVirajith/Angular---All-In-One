import { ClickListenerDirective } from './click-listener.directive';

describe('ClickListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickListenerDirective();
    expect(directive).toBeTruthy();
  });
});
