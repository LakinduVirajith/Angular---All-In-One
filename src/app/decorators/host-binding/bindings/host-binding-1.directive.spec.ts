import { HostBinding1Directive } from './host-binding-1.directive';

describe('HostBinding1Directive', () => {
  it('should create an instance', () => {
    const directive = new HostBinding1Directive();
    expect(directive).toBeTruthy();
  });
});
