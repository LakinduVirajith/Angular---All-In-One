import { HostBinding2Directive } from './host-binding-2.directive';

describe('HostBinding2Directive', () => {
  it('should create an instance', () => {
    const directive = new HostBinding2Directive();
    expect(directive).toBeTruthy();
  });
});
